# Example .env for saccade integration tests
# Copy to packages/saccade/.env and fill in your values

# LiteLLM model string (required for cassette recording)
# Examples:
#   PROVIDER_MODEL=openai/gpt-4o-mini
#   PROVIDER_MODEL=my-provider/glm-4.7
PROVIDER_MODEL=openai/gpt-4o-mini

# Custom OpenAI-compatible provider settings (required if using custom provider)
# PROVIDER_API_BASE=https://api.example.com/v1
# PROVIDER_API_KEY=your-api-key
